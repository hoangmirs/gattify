# Linux

The intended backend uses BlueZ D-Bus, explicit adapter selection, exported
GATT application ownership and reliable daemon restart cleanup.

No concrete Linux radio backend is implemented. Peer-profile hosting must
remain unavailable until a supported API can prove delivery to one selected
subscriber without silent broadcast.

