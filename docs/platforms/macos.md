# macOS

macOS is a separate target from the iOS Swift bridge. The intended backend uses
CoreBluetooth with retained delegates and run-loop/queue ownership. Privacy
usage descriptions and sandbox entitlements are consumer configuration.

No concrete macOS radio backend is implemented or compiled yet.

