# Hardware result records

Add one immutable Markdown or JSON record per run containing date, commit, OS,
device and adapter, driver, build/package type, radio roles, duration, message
sizes/counts, p50/p95 round-trip time, failures and reconnect results.

No hardware test has been performed for the current implementation.

