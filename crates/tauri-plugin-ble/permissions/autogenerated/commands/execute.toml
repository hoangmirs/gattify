[[permission]]
identifier = "allow-execute"
description = "Allows dispatch through the typed BLE command envelope."
commands.allow = ["execute"]

[[permission]]
identifier = "deny-execute"
description = "Denies dispatch through the typed BLE command envelope."
commands.deny = ["execute"]

